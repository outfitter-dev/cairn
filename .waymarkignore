# Waymark-specific ignore patterns
# These are applied in addition to patterns from .waymark/config.json

# Only ignore build artifacts and dependencies
node_modules/
**/dist/
**/*.log
**/*.map

# Keep it minimal - we want to find waymarks!